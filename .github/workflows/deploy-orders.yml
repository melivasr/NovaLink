name: Deploy Orders Service

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'services/orders/**'
      - '.github/workflows/deploy-orders.yml'

concurrency:
  group: deploy-orders-${{ github.ref }}
  cancel-in-progress: false

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: services/orders/package-lock.json

      - name: Install dependencies
        working-directory: services/orders
        run: npm install

      # - name: Run tests
      #   working-directory: services/orders
      #   run: npm test --if-present

      - name: Lint code
        working-directory: services/orders
        run: npm run lint --if-present

      - name: Build Docker image
        working-directory: services/orders
        run: |
          docker build -t novalink/orders-service:${{ github.sha }} .
          docker tag novalink/orders-service:${{ github.sha }} novalink/orders-service:latest

      - name: Push Docker image (if registry configured)
        run: |
          # docker push novalink/orders-service:${{ github.sha }}
          # docker push novalink/orders-service:latest
          echo "Docker image built successfully"

      - name: Update Kubernetes manifest
        run: |
          sed -i "s|novalink/orders-service:latest|novalink/orders-service:${{ github.sha }}|g" k8s/deployments/orders.yaml

      - name: Commit and push manifest changes
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          git add k8s/deployments/orders.yaml
          git commit -m "Update orders-service image to ${{ github.sha }}" --allow-empty
          git pull --rebase origin ${{ github.ref_name }}
          git push

      - name: Deploy to Kubernetes (if cluster configured)
        run: |
          # kubectl apply -f k8s/deployments/orders.yaml
          echo "Kubernetes deployment ready"
