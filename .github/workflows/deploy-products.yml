name: Deploy Products Service

on:
  push:
    branches:
      - main
      - develop
    paths:
      - 'services/products/**'
      - '.github/workflows/deploy-products.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: services/products/package-lock.json

      - name: Install dependencies
        working-directory: services/products
        run: npm install

      - name: Run tests
        working-directory: services/products
        run: npm test --if-present

      - name: Lint code
        working-directory: services/products
        run: npm run lint --if-present

      - name: Build Docker image
        working-directory: services/products
        run: |
          docker build -t novalink/products-service:${{ github.sha }} .
          docker tag novalink/products-service:${{ github.sha }} novalink/products-service:latest

      - name: Push Docker image (if registry configured)
        run: |
          # docker push novalink/products-service:${{ github.sha }}
          # docker push novalink/products-service:latest
          echo "Docker image built successfully"

      - name: Update Kubernetes manifest
        run: |
          sed -i "s|novalink/products-service:latest|novalink/products-service:${{ github.sha }}|g" k8s/deployments/products.yaml

      - name: Commit and push manifest changes
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "GitHub Actions"
          git add k8s/deployments/products.yaml
          git commit -m "Update products-service image to ${{ github.sha }}" --allow-empty
          git push

      - name: Deploy to Kubernetes (if cluster configured)
        run: |
          # kubectl apply -f k8s/deployments/products.yaml
          echo "Kubernetes deployment ready"
